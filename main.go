package main

import (
	"fmt"
	"net/http"
	"time"
)

func main() {
	http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
		// Этот код выполняется каждый раз, когда кто-то заходит на сайт
		fmt.Fprintf(w, "<h1>🐯 Привет! Я настоящий Go-сервер!</h1>")
		fmt.Fprintf(w, "<p>Сейчас: %s</p>", time.Now().Format("15:04:05"))
		fmt.Fprintf(w, "<p>Меня собрали и запустили автоматически через Docker.</p>")
	})

	fmt.Println("Сервер слушает порт :80")
	// Важно: слушаем порт 80, потому что GoShip ждет сайт именно там
	http.ListenAndServe(":80", nil)
}
